#include "List.c"

int main()
{/*
	List *p_list = list_init(2);
	print_list(p_list);
	push(p_list, 10);
	push(p_list, 20);
	print_list(p_list);
	unshift(&p_list, 50);
	unshift(&p_list, -50);
	push(p_list, 6);
	print_list(p_list);
	insert_after(p_list, 100, 3);
	print_list(p_list);
	log_list(p_list);
	pop(&p_list);
	pop(&p_list);
	pop(&p_list);
	pop(&p_list);
	pop(&p_list);
	pop(&p_list);
	pop(&p_list);
	return 0;*/
}
